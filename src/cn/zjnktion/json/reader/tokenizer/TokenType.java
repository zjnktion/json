package cn.zjnktion.json.reader.tokenizer;

/**
 * @author zjnktion
 */
public enum TokenType {

    OBJ_PREFIX, OBJ_SUFFIX, ARRAY_PREFIX, ARRAY_SUFFIX, NULL, NUMBER, STRING, BOOLEAN, COLON, COMMA, FINISH;
}
